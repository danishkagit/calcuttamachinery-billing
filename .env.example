# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB (local instance)
MONGODB_URI=mongodb://localhost:27017/gstbilling

# JWT Secret (change for production)
JWT_SECRET=calcutta_machinery_jwt_secret_2024
JWT_EXPIRE=30d

# Production (Render)
# CLIENT_URL=https://your-frontend.vercel.app

# Production (MongoDB Atlas)
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/gstbilling?retryWrites=true&w=majority

# Production (Vercel - set in Vercel dashboard)
# REACT_APP_API_URL=https://your-backend.onrender.com/api